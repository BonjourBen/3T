{
  "devDependencies": {
    "esbuild": "0.24.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "compile": "npx tsc",
    "minify": "esbuild dist/app.js --minify --outfile=dist/app.min.js",
    "watch": "chokidar \"src/**/*\" -c \"npm run compile && npm run minify\""
  }
}
